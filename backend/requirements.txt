# AralRiskAI — lightweight Vercel API requirements.
# For full local ML training, SHAP and Earth Engine support, use:
#   pip install -r requirements-full.txt

fastapi==0.111.0
pydantic==2.7.1
numpy==1.26.4
